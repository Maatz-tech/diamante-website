---
import Icon from './Icon.astro';

interface Props {
  icon: string;
  title: string;
  description: string;
  class?: string;
}

const { icon, title, description, class: className = '' } = Astro.props;
---

<article
  data-card
  class={`card group/card flex h-full flex-col gap-10 p-6 transition-shadow duration-300 hover:shadow-[0_12px_32px_rgba(21,41,109,0.10)] ${className}`}
>
  <span
    class="flex w-fit items-center justify-center rounded-xl bg-blue p-3 text-white transition-transform duration-300 group-hover/card:-translate-y-0.5 group-hover/card:scale-105"
  >
    <Icon name={icon} class="size-6" />
  </span>

  <div class="flex flex-col gap-3 text-[#3c3c42]">
    <h3 class="text-xl/[1.2] font-medium">{title}</h3>
    <p class="text-base/[1.25]">{description}</p>
  </div>
</article>
