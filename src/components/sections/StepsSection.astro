---
import SectionHeader from '../SectionHeader.astro';
import { STEPS } from '../../data/site';
---

<section id="etapas" class="relative isolate overflow-hidden bg-off-white">
  <!-- Onda decorativa que liga as etapas (apenas desktop) -->
  <img
    src={`${import.meta.env.BASE_URL}images/wave.svg`}
    alt=""
    width="1477"
    height="176"
    aria-hidden="true"
    loading="lazy"
    class="pointer-events-none absolute left-1/2 top-1/2 z-0 hidden w-[1477px] max-w-none -translate-x-1/2 translate-y-[calc(-50%+72px)] xl:block"
  />

  <div
    class="container-page relative z-10 flex flex-col items-center gap-10 py-16 lg:gap-16 lg:py-20"
  >
    <SectionHeader
      eyebrow="Etapas do Processo Seletivo"
      title="Sua jornada começa aqui"
      align="center"
    />

    <ol
      class="grid w-full gap-9 xl:w-[1086px] xl:grid-cols-3 xl:gap-x-20 xl:gap-y-20"
    >
      {
        STEPS.map((step, index) => (
          <li data-card class="relative flex flex-col items-center justify-center gap-4 rounded-2xl border border-blue bg-white px-5 py-5 text-center xl:items-start xl:gap-[26px] xl:py-6 xl:pl-10 xl:pr-5 xl:text-left">
            {/* Badge numerada: topo no mobile, lateral esquerda no desktop */}
            <span
              data-pop
              class="absolute -top-7 left-1/2 flex size-14 -translate-x-1/2 items-center justify-center rounded-full bg-[rgba(32,57,249,0.1)] xl:left-0 xl:top-1/2 xl:-translate-x-1/2 xl:-translate-y-1/2"
            >
              <span
                class="flex size-11 items-center justify-center rounded-full bg-blue text-2xl font-medium text-white"
              >
                {index + 1}
              </span>
            </span>

            <div class="mt-3 flex flex-col gap-2 xl:mt-0">
              <p class="text-base/[1.2] text-[#3c3c42]">{step.title}</p>
              <p
                class="whitespace-nowrap text-base/[1.2] font-medium uppercase tracking-[-0.01em] text-navy lg:whitespace-nowrap"
              >
                {step.date}
              </p>
            </div>
          </li>
        ))
      }
    </ol>
  </div>
</section>

<style>
  /* Linha vertical que conecta as etapas no mobile */
  @media (max-width: 1023px) {
    ol {
      position: relative;
    }
    ol::before {
      content: '';
      position: absolute;
      left: 50%;
      top: 0;
      bottom: 0;
      width: 2px;
      transform: translateX(-50%);
      background: rgba(32, 57, 249, 0.2);
      z-index: -1;
    }
  }
</style>
