---
interface Props {
  href?: string;
  variant?: 'primary' | 'secondary' | 'outline' | 'on-brand' | 'hero';
  /** Ocupa 100% da largura do container (usado no mobile). */
  block?: boolean;
  type?: 'button' | 'submit';
  class?: string;
}

const {
  href,
  variant = 'primary',
  block = false,
  type = 'button',
  class: className = '',
} = Astro.props;

const variantClass = {
  primary: 'btn-primary',
  secondary: 'btn-secondary',
  outline: 'btn-outline',
  'on-brand': 'btn-on-brand',
  hero: 'btn-hero',
}[variant];

const classes = ['btn', variantClass, block ? 'w-full' : '', className]
  .filter(Boolean)
  .join(' ');

// Links externos (inscrição) abrem em nova aba, preservando o hotsite aberto.
const isExternal = href?.startsWith('http');
---

{
  href ? (
    <a
      href={href}
      class={classes}
      target={isExternal ? '_blank' : undefined}
      rel={isExternal ? 'noopener noreferrer' : undefined}
    >
      <slot />
    </a>
  ) : (
    <button type={type} class={classes}>
      <slot />
    </button>
  )
}
